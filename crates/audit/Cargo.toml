[package]
name = "rngo-audit"
version = "0.36.0"
edition = "2024"
description = "Signal evaluation and audit reporting for rngo simulations"
license = "MIT OR Apache-2.0"
repository = "https://github.com/rngodev/rngo-rs"
homepage = "https://rngo.dev"
keywords = ["simulation", "testing", "data", "codegen"]
categories = ["development-tools::testing", "simulation"]

[dependencies]
rngo-core = { version = "0.36.0", path = "../core" }
rngo-log = { version = "0.36.0", path = "../log" }
cel = { version = "0.14.0", features = ["chrono"] }
indexmap = { version = "2.14.0", features = ["serde"] }
serde = { version = "1.0.217", features = ["derive"] }
serde_json = "1.0.138"

[dev-dependencies]
rand_pcg = { version = "0.10.2", features = ["serde"] }
